/*using UnityEditor;
using UnityEngine;

namespace RPGSystem
{
    [CustomEditor(typeof(Database))]
    public class DatabaseInspector : Editor
    {
        public override void OnInspectorGUI()
        {
            GUILayout.Space(Constant.SPACE_HIGH);

            EditorGUILayout.BeginHorizontal();
            {
                GUILayout.FlexibleSpace();
                if (GUILayout.Button("Open Database", GUILayout.MinHeight(30f), GUILayout.MaxWidth(230f)))
                {
                    MainWindowEditor.Init();
                }
                GUILayout.FlexibleSpace();
            }
            EditorGUILayout.EndHorizontal();

            GUILayout.Space(Constant.SPACE_HIGH);
        }
    }
}
*/
