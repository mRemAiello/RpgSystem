using UnityEditor;
using UnityEngine;

/*namespace RPGSystem
{
    [CustomEditor(typeof(CategoryData))]
    public class CategoryDataEditor : Editor
    {
        private string tmp_name = "";
        private Vector2 scrollingPosition = Vector2.zero;

        public override void OnInspectorGUI()
        {


            /*Name = CustomEditorGUILayout.TextField("Name", Name);

            scrollingPosition = GUILayout.BeginScrollView(scrollingPosition, GUILayout.ExpandWidth(true), GUILayout.ExpandHeight(true));
            CustomEditorGUILayout.BeginVerticalBox("Subcategories", true, true);
            GUILayout.Space(Constant.SPACE_HEADER);
            for (int i = 0; i < subcategories.Count; i++)
            {
                GUILayout.BeginHorizontal();
                subcategories[i] = CustomEditorGUILayout.TextField("Category Name:", subcategories[i]);
                if (CustomEditorGUILayout.SquaredButton("X"))
                    subcategories.RemoveAt(i);
                GUILayout.EndHorizontal();
                GUILayout.Space(Constant.SPACE_LIGHT);
            }
            CustomEditorGUILayout.EndVerticalBox();
            GUILayout.EndScrollView();

            GUILayout.BeginHorizontal();
            tmp_name = CustomEditorGUILayout.TextField("", tmp_name);
            if (CustomEditorGUILayout.Button("Add"))
                subcategories.Add(tmp_name);
            GUILayout.EndHorizontal();
        }
    }
}*/

